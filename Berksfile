source 'https://supermarket.chef.io'

metadata

group :test do
  cookbook 'docker', '~> 5.0'
  cookbook 'ark', '~> 5.0'
  cookbook 'seven_zip', '~> 3.1.2'
  cookbook 'docker', '~> 5.0'
  cookbook 'yum'
  cookbook 'test', path: 'test/fixtures/cookbooks/test'
  cookbook 'cluster', path: 'test/fixtures/cookbooks/cluster'
end
